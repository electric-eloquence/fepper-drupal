'# content\?': |2
  {%
    set body_classes = [
      logged_in ? 'user-logged-in',
      not root_path ? 'path-frontpage' : 'path-' ~ root_path|clean_class,
      node_type ? 'page-node-type-' ~ node_type|clean_class,
      db_offline ? 'db-offline',
    ]
  %}
'html_attributes': |2
  \{\{ html_attributes \}\}
'placeholder_token': |2
  \{\{ placeholder_token \}\}
'head_title': |2
  \{\{ head_title|safe_join(' | ') \}\}
'body_attributes': |2
  \{\{ attributes.addClass(body_classes) \}\}
'skip': |2
  \{\{ 'Skip to main content'|t \}\}
'page_top': |2
  \{\{ page_top \}\}
'page': |2
  \{\{ page \}\}
'page_bottom': |2
  \{\{ page_bottom \}\}
